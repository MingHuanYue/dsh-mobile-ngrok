# 开发记录

给自己留的备忘录。别人看这个仓库的话，直接看 [README](README.md) 就行。

---

## 目录结构

代码放在一个**工作区**里，仓库只是工作区的一个子目录：

```
工作区/
├── mingyue-dsh/          ← 这个仓库
│   ├── android/  gateway/  compat/  docs/
│   └── sync-into-repo.js
│
└── _buildtools/          ← 不进仓库
    ├── paths.py          路径解析（compat 脚本共用）
    ├── legacy-polyfill.js 等注入用的源文件
    ├── snapshots/        改动前的备份
    └── 各种临时诊断脚本
```

工作区根目录**不写死**。`sync-into-repo.js` 和 `compat/paths.py` 都是
从自己所在位置往上推，也都可以用环境变量覆盖：

```powershell
set MINGYUE_WS=D:\你的目录     # 工作区根
set DSH_NM=...\node_modules\@deepseek-ai   # DSH 的包目录
```

`compat/paths.py` 会自己去找 DSH 装在哪（`.dsh/profiles`、npm 全局、npx 缓存
三个地方挨个试），找不到会明确报错并列出试过的路径，不会静默用错路径。

推送走的是本机已经登录的 `gh`。全局 gitconfig 里给 github.com 配了
`gh auth git-credential` 作为凭据助手，所以 `git push` 不需要另外给 token。

---

## 改完东西之后

```powershell
cd mingyue-dsh
node sync-into-repo.js            # 先预演，看收了哪些、挡了哪些
node sync-into-repo.js --write    # 确认没问题再真写
git add -A
git commit -m "..."
git push
```

**注意改文件的顺序**：`compat/` 下的脚本在两个地方各有一份（工作区的
`_buildtools/` 和仓库的 `compat/`），同步会在两者之间复制。
**只改一份的话，下次同步就把改动覆盖回去了。** 这个坑踩过，丢了半小时的改动。
改这类共享文件，改完两个位置都要确认一遍。

`sync-into-repo.js` 里有两份清单：`COPY` 是要同步的，`FORBIDDEN` 是要挡的。
往仓库里加新文件的话，记得往 `COPY` 里补一行，别直接手工复制，
不然下次同步会被覆盖掉或者漏掉。

---

## 提交前自己查一遍

这些不是形式，每一条都对应一次真实事故。

```powershell
# 有没有令牌混进来
git log -p | Select-String "authtoken|private key"

# 有没有不该有的文件
git ls-files | Select-String "ngrok.yml|keystore|\.exe$|\.apk$|\.log$"
```

**踩过的坑**：有一次我在文档里写了两串 ngrok 令牌，本意是提醒去吊销它们，
结果那份文档一起被提交推上去了。审计脚本只查了文件名和旧令牌特征，
没料到我自己刚写的内容里带着新令牌。

后来是用 git 的底层命令重写了全部历史才清干净的（`git filter-branch` 不行，
它内部会调 `sh.exe`，本机沙箱不让）。重写之后还要把
`refs/remotes/origin/main` 指向的旧提交也清掉，否则它仍然可达、`gc` 回收不到。

---

## 发版

APK 不放源码仓库，走 Release：

```powershell
gh release create v1.0.0 path\to\mingyue-dsh.apk `
  --title "DSH 手机端 · 手机端 DeepSeek Harness v1.0.0" `
  --notes "..."
```

---

## 仓库里刻意没放的东西

原因都写在 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) 里了，这里只列名字：

- `ngrok.exe`，专有软件，32 MB
- 挂件的美术素材，原作者声明不授予再许可
- `*.apk`、`*.keystore`
- DSH 前端那些被改过的产物，只留能复现改动的脚本
