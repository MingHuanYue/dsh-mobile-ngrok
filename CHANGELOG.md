# 更新日志

## v1.0.0

首个公开版本。手机端 + 电脑端网关，用 ngrok 隧道把电脑上跑的 DSH 搬到手机上。

### 手机端

**WebView 外壳**
- 复用 DSH 自己的 `?token=` 登录，不额外存密码
- 地址与令牌可在设置里改，也支持 `adb` 用启动参数写入
- 关掉长按选中，免得打断输入

**兼容旧浏览器内核**
手机自带的内核往往比 DSH 前端要求的版本低。差距分两类，修法不同：
- 语法层（类静态块 `static{}`）——解析期就报错，只能转译
- 运行时层（`Promise.withResolvers`、`AbortSignal.any` 等）——用 polyfill 补

**窄屏排版**
设置对话框在手机上是竖排/挤成一团的，逐项量过之后修正：
- 左导航宽度实测定为 120px（每个标签需要 56px + 图标 16px + 间距 8px；
  试过 112px 会把「Agent 预设」截断）
- 设置行的标签列原本有 `padding-right: 48px`，把文字挤到只剩 51px，
  压到 8px 之后「字号大小」从折 2 行变成 1 行

**保活与恢复**
- 息屏/切后台之后，页面的 `visibilityState` 有时会永久停在 `hidden`，
  浏览器因此停掉渲染。回到前台时会重新设置可见性并补发生命周期
- 渲染进程被系统回收后重建 WebView，而不是整个 App 退出

### 电脑端网关

**令牌门禁**
DSH 本身只监听 `127.0.0.1`（这是故意的，开放到公网等于把电脑交出去）。
网关挡在前面，每个进来的请求都要先过令牌，再转发给 DSH。

**静态资源缓存头**（这一版最主要的改动）
DSH 的前端服务对静态资源不发 `Cache-Control`、`ETag`、`Last-Modified`，
浏览器无法判断资源变没变，于是每次打开 App 都完整重下一遍。

用手机 WebView 调试口实测的数据：一次冷启动整页 0.91 MB，其中挂件的
图片/脚本/音效占 0.80 MB（88%），而且同一张图还被请求了两次。

网关在转发 `200` 响应时按路径补上缓存头：

| 路径 | 依据 | 缓存时长 |
|---|---|---|
| `/assets/` | 文件名带构建时算的内容哈希 | 1 年，immutable |
| `/plugins/` | URL 带 `&rev=` 版本号 | 7 天 |
| `/dsh-whale/` | 挂件素材，基本不变 | 30 天 |

首页 HTML 与 `/api` 明确不缓存。

**实测效果**：整页传输 0.91 MB → 0.10 MB。

### 已知问题

- **部分机型长时间息屏后需要重开**：WebView 把 `visibilityState` 留在
  `hidden`，这是内核行为，应用侧改不掉
- **横屏不套用窄屏排版**：媒体查询上限是 520px，横屏视口通常超过
- **ngrok 免费版有额度**：每月 1 GB 传输 + 20,000 次请求，超出后服务暂停
  （不会自动扣费），下个月重置

### 许可

代码 MIT。仓库不包含第三方二进制（ngrok）、挂件美术素材、APK 签名私钥，
详见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。
